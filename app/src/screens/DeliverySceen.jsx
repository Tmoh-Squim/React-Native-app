import { View, Text } from "react-native";
import React from "react";
import Delivery from "../user/DeliveryDetails"
export default function DeliverySceen({route}) {
  return (
   <Delivery route={route} />
  );
}
